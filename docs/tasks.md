# Task Backlog

The backlog now lives in per-task files under [`docs/tasks/`](tasks/).

Start with [tasks/agent-index.md](tasks/agent-index.md).

This file intentionally stays small so agents do not load the entire backlog when one task brief is enough.
