# Task Backlog

The backlog now lives in per-task files under the status folders in [`docs/tasks/`](tasks/).

Start with [tasks/agent-index.md](tasks/agent-index.md), then follow the relevant status-folder index.

This file intentionally stays small so agents do not load the entire backlog when one task brief is enough.
